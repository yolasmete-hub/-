# X (Twitter) Developer Platform OAuth credentials
# Create an app at https://developer.x.com and copy its OAuth 2.0 credentials here.
# Copy this file to `.env` and fill in real values. `.env` is gitignored.

X_CLIENT_ID=your-x-oauth2-client-id
X_CLIENT_SECRET=your-x-oauth2-client-secret
